{
    "name": "danielvm-git/bigbase-canary-php",
    "description": "Minimal PHP canary proving big-release + bigbase-deploy interop",
    "type": "project",
    "require": {
        "php": ">=8.3"
    },
    "authors": [
        {
            "name": "danielvm-git",
            "email": "danielvm@gmail.com"
        }
    ],
    "require-dev": {
        "phpunit/phpunit": "^11"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "lint": "php -l index.php && php -l src/Footer.php"
    }
}
