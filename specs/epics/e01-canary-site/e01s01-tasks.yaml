story: e01s01
bcps: 5
tasks:
  - id: t1
    title: Write failing PHPUnit test for footer content
    verify: "composer test 2>&1 | grep -i fail"
  - id: t2
    title: Implement index.php + extracted footer function to pass the test
    verify: "composer test"
  - id: t3
    title: Add .big-release.yml
    verify: "test -f .big-release.yml && grep -q tagFormat .big-release.yml"
  - id: t4
    title: Author test-build-release.yml + deploy.yml from scratch (Python's shape, Composer/PHPUnit)
    verify: "! grep -rn CHANGE-ME .github/workflows/*.yml"
  - id: t5
    title: Provision bigbase site + GitHub secrets
    verify: "gh secret list -R danielvm-git/bigbase-canary-php | grep -q BIGBASE_SITE_ID"
  - id: t6
    title: Land on main, confirm green CI + live deploy (content-verified)
    verify: "curl -s https://php.bigbase.click | grep -q 'bigbase canary (PHP)'"
